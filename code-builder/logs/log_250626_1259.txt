(openwebui-env) (base) andre@Andres-MacBook-Air code-builder % python -m src.main

What function should I build? please write function to check if string is palindrome
[orchestrator] wrote spec
[qa] wrote tests
[coder] wrote code (attempt 1)
[sandbox] passed: False
[router] tests failed - retrying (attempt 1/3)
[coder] wrote code (attempt 2)
[sandbox] passed: True

=== spec ===
```markdown
# Palindrome Checker Function
## Function Name and Signature
### `is_palindrome(s: str) -> bool`

## One-line Description
Checks if a given string is a palindrome.

## Edge Cases
- Empty strings are palindromes.
- Single character strings are always palindromes.

## Concrete Input->Output Examples
```python
# Valid input
print(is_palindrome("madam"))  # Output: True

# Invalid input (non-string)
try:
    print(is_palindrome(123))
except TypeError as e:
    print(e)  # Output: Expected str, got int

# Invalid input (string with non-alphanumeric characters)
print(is_palindrome("a man a plan a canal panama"))  # Output: True
```

=== code ===
def is_palindrome(s: str) -> bool:
    """
    Checks if a given string is a palindrome.

    Args:
        s (str): The input string to check.

    Returns:
        bool: True if the string is a palindrome, False otherwise.
    """
    # Check if input is not a string
    if not isinstance(s, str):
        raise TypeError("Expected str, got {}".format(type(s).__name__))
    
    # Remove non-alphanumeric characters and convert to lowercase
    cleaned_s = ''.join(c for c in s if c.isalnum()).lower()
    
    # Compare the cleaned string with its reverse
    return cleaned_s == cleaned_s[::-1]

=== tests ===
import pytest
from solution import is_palindrome

def test_empty_string():
    assert is_palindrome("") == True

def test_single_character_string():
    assert is_palindrome("a") == True

def test_valid_input():
    assert is_palindrome("madam") == True

def test_invalid_input_non_string():
    with pytest.raises(TypeError) as e:
        is_palindrome(123)
    assert str(e.value) == "Expected str, got int"

def test_invalid_input_string_with_non_alphanumeric_characters():
    assert is_palindrome("a man a plan a canal panama") == True

=== status: ok

Build succeeded.

Build another? [y/N]: 
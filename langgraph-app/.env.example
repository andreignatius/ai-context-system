# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Langfuse observability (optional — sign up at https://cloud.langfuse.com or self-host)
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# Ollama model to use
OLLAMA_MODEL=llama3.2:latest
